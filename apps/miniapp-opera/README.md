# 沂蒙小戏小剧小程序

第一阶段目标：在现有 `xiao-opera-miniapp` 基础上重构为可独立演示的微信小程序体验层，通过 API Client + Mock 数据与业务后台解耦。

## 页面基线

- 首页
- 剧目列表 / 详情
- 展演列表 / 活动详情
- 直播列表 / 详情
- 演员列表 / 详情
- 剧团列表 / 详情
- 搜索
- 我的

## 开发边界

页面不得直接调用 `wx.request`。所有网络访问必须通过统一 API Client 层。旧仓库仅作为迁移与复用来源，不直接继承其产品结构。
