/* 史料馆：浅色档案工作台（V0.2 §4 范式——检索框突出、表格化著录、蓝灰筛选） */
.container { padding: 32rpx; padding-bottom: 56rpx; }

.workbench-head { margin-bottom: 24rpx; }
.wb-title { font-size: 40rpx; font-weight: 700; color: var(--ymh-ink); font-family: "Songti SC", "STSong", serif; }
.wb-sub { margin-top: 8rpx; font-size: 24rpx; color: var(--ymh-slate-600); }

.search-box { margin-top: 24rpx; display: flex; gap: 16rpx; }
.search-input { flex: 1; background: var(--ymh-surface); border: 1rpx solid var(--ymh-stone); border-radius: 8rpx; padding: 20rpx 24rpx; font-size: 28rpx; color: var(--ymh-text); }
.search-btn { background: var(--ymh-slate-700); color: #FFFFFF; border-radius: 8rpx; padding: 0 36rpx; display: flex; align-items: center; font-size: 28rpx; }

.type-tabs { margin-top: 24rpx; white-space: nowrap; }
.type-tab { display: inline-block; padding: 12rpx 28rpx; margin-right: 12rpx; border-radius: 4rpx; background: var(--ymh-slate-100); color: var(--ymh-slate-700); font-size: 24rpx; }
.type-tab--on { background: var(--ymh-slate-700); color: #FFFFFF; }

/* 检索工作台第二层筛选：年代/等级（档案蓝灰语义） */
.filter-row { display: flex; align-items: center; margin-top: 16rpx; }
.filter-label { flex-shrink: 0; width: 72rpx; font-size: 22rpx; color: var(--ymh-slate-600); }
.filter-row .type-tabs { margin-top: 0; flex: 1; }
.type-tab--slate { background: var(--ymh-surface); border: 1rpx solid var(--ymh-paper-deep); color: var(--ymh-slate-600); padding: 8rpx 20rpx; font-size: 22rpx; }
.type-tab--era-on { background: var(--ymh-red-800); border-color: var(--ymh-red-800); color: #F2ECDF; }

.state { margin-top: 96rpx; text-align: center; color: var(--ymh-text-2); font-size: 26rpx; }
.retry-btn { margin: 24rpx auto 0; width: 200rpx; padding: 14rpx 0; border: 1rpx solid var(--ymh-slate-600); border-radius: 8rpx; color: var(--ymh-slate-700); font-size: 26rpx; }

/* ArchiveCard：缩略以类型色块占位（真实封面接入后替换为史料图，按"历史单色"规则处理） */
.archive-card { margin-top: 20rpx; background: var(--ymh-surface); border: 1rpx solid var(--ymh-paper-deep); border-radius: 12rpx; box-shadow: var(--ymh-shadow-rest); padding: 24rpx; display: flex; gap: 20rpx; }
.ac-thumb { width: 140rpx; height: 140rpx; border-radius: 8rpx; flex-shrink: 0; display: flex; align-items: center; justify-content: center; }
.ac-type { font-size: 24rpx; color: var(--ymh-paper); font-family: "Songti SC", "STSong", serif; }
.ac-thumb--document { background: var(--ymh-slate-600); }
.ac-thumb--image, .ac-thumb--press { background: var(--ymh-stone); }
.ac-thumb--file { background: var(--ymh-slate-700); }
.ac-thumb--oral-history, .ac-thumb--audio { background: var(--ymh-red-800); }
.ac-thumb--video { background: var(--ymh-red-900); }

.ac-body { flex: 1; min-width: 0; }
.ac-title { font-size: 30rpx; font-weight: 650; color: var(--ymh-ink); line-height: 1.4; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
.ac-meta { margin-top: 10rpx; display: flex; align-items: center; gap: 16rpx; }
.ac-era { font-size: 22rpx; color: var(--ymh-slate-600); font-variant-numeric: tabular-nums; }

/* 来源等级徽章：只显示中文标签（V0.2 §6.1） */
.grade-badge { padding: 4rpx 14rpx; border-radius: 4rpx; font-size: 22rpx; }
.grade-badge--A { background: var(--ymh-grade-a-bg); color: var(--ymh-grade-a-fg); }
.grade-badge--B { background: var(--ymh-grade-b-bg); color: var(--ymh-grade-b-fg); }
.grade-badge--C { background: var(--ymh-grade-c-bg); color: var(--ymh-grade-c-fg); }
.grade-badge--D { background: var(--ymh-grade-d-bg); color: var(--ymh-grade-d-fg); }

.ac-summary { margin-top: 10rpx; font-size: 24rpx; color: var(--ymh-text-2); line-height: 1.5; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }

.seed-note { margin-top: 32rpx; text-align: center; font-size: 22rpx; color: var(--ymh-text-3); }
